# 実装フロー（引き継ぎ版）

## いまの前提
- 1マス = 112px
- 画像サイズと占有マスは別管理
- 小物は1マスに複数置ける
- 森の状態は `js/core-runtime.js` が持つ
- 画面起動は `index.html` + `js/app.js`

## 実装済みの役割
- `assets.json` : 素材一覧
- `spots.json` : 置き場所一覧
- `map.json` : 森の骨格
- `events.json` : 成長イベント
- `js/core-runtime.js` : 保存・動物・ショップの中核
- `js/camera.js` : スクロールとズーム
- `js/placement.js` : 配置
- `js/render.js` : 描画
- `js/interaction.js` : クリック・ドラッグ
- `js/app.js` : 起動と接続
- `index.html` : 画面

## 次に足しやすいもの
- `js/season.js` の本格利用
- `js/badge.js` の画面
- `js/audio.js` の音イベント接続
- ショップの画面分離
- ミニマップ
- 動物の個別アニメーション

## 配置物タップ詳細ポップアップ(v16で実装)
- 花などをタップすると「だれが/いつ/どんな目標をクリアして」置いたかのポップアップが出る
- 詳細は `docs/13_placed_info_popup.md` を参照

## 正規版への整理(進行中)
- [済] 先生用機能(承認待ち・クラス連携設定)を `teacher.html` に分離
- [済] 児童画面(`index.html`)から開発者用ツール(ポイント追加・動物を強制出現・グリッド/スポット切替)、
  プレゼン用デモ再生、テスト用「ありがとうを受け取ってみる」ボタンを削除
- [次候補] トップバー/画面全体のレイアウト整理、見た目(絵本風デザイン)の刷新
