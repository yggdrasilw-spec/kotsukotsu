# 起動方法

1. `index.html` をブラウザで開く
2. もしローカルで fetch が動かない場合は、静的サーバーで開く
3. `assets.json / spots.json / map.json / events.json` は `data/` に置く
4. 画面右側のアセットを選んで、森をクリックして配置する
5. 保存、読込、書き出しで状態を持ち回る

## 使い方のポイント
- 小物は1マスに複数置ける
- 動物はボタンから出せる
- ズームはホイールでも操作できる
- グリッドとスポットはデバッグ表示として切り替え可能
